Download and install npm packages using this command :
- npm install

.. and angular pagination with this command:
- npm install ngx-pagination --save

then you can run this application locally with:
- ng serve --open